eureka:
  instance:
    hostname: discovery-server-3
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://discovery-server-1:${server.port}/eureka/,http://discovery-server-2:${server.port}/eureka/
